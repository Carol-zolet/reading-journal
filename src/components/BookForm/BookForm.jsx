import React from "react";
import { Typography, Paper } from "@mui/material";

// Este é um componente provisório apenas para os testes passarem.
const BookForm = () => {
  return (
    <Paper elevation={2} sx={{ padding: 2, marginTop: 3 }}>
      <Typography variant="h6">Formulário de Livro</Typography>
    </Paper>
  );
};

export default BookForm;
